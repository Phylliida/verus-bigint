Hardened `scripts/check.sh` CI preflight to enforce fail-fast workflow steps (`set -euo pipefail`, no `continue-on-error: true`, no `|| true`), documented the guardrails, revalidated strict and rug-oracle checks, and 2 roadmap tasks remain.
